import 'package:flutter/material.dart';

class AristPage extends StatelessWidget {
  const AristPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
